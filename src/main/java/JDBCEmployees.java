import com.mysql.cj.jdbc.Driver;

import java.sql.*;
import java.util.ArrayList;

public class JDBCEmployees {
    public static void main(String[] args) {
//        try {
//            DriverManager.registerDriver(new Driver());

//        Must use codeup_test_db for getConnection. Config is currently set to aadlister_db.

//            Statement statement = connection.createStatement();

//            ResultSet resultSet = statement.executeQuery("SELECT * FROM employees LIMIT 10");

//            Commented out below: Printing first 10 FIRST NAMES from employee DB

//            while (resultSet.next()) {
//                System.out.println(resultSet.getString("first_name"));
//            }

//            Below: Adding first 10 FIRST NAMES to an ArrayList

//            ArrayList<String> tenNames = new ArrayList<>();
//            while (resultSet.next()) {
//                tenNames.add(resultSet.getString("first_name"));
//            }
//            System.out.println(tenNames);

//        } catch (SQLException throwables) {
//            throwables.printStackTrace();
//        }
    }
}
